{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://littledays.app/schemas/tutor/lesson.schema.json",
  "title": "Aliz Tutor lesson",
  "description": "One deterministic 5-minute (or shorter starter) lesson for Aliz Tutor Mode. The LessonEngine owns pass/fail, progression and rewards from this data alone; an LLM may only rephrase wording later and never changes expectedAnswers, stars or order. All keys are camelCase. Text is English, ASCII-printable, and short enough to be spoken as one TutorTurn (speech <= 160 chars, question <= 120 chars). Rules that depend on the step kind live in x-requiredByKind and x-forbiddenByKind so the validator and this file cannot drift.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "lessonId", "subjectId", "title", "ageBand", "targetDurationSeconds", "language", "helperLanguageKeys", "steps", "completion"],
  "properties": {
    "schemaVersion": { "type": "integer", "const": 1 },
    "lessonId": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_]*$",
      "description": "Must equal the file name without .json and appear in exactly one subject's lessonIds."
    },
    "subjectId": { "type": "string", "pattern": "^[a-z][a-z0-9_]*$" },
    "title": { "type": "string", "minLength": 1, "maxLength": 60 },
    "ageBand": { "type": "string", "enum": ["3-4", "4-6", "3-6"] },
    "targetDurationSeconds": {
      "type": "integer",
      "minimum": 30,
      "maximum": 600,
      "description": "300 for a full session. The validator's estimate (6 s per teach, 12 s per ask plus a 10 s retry allowance, 10 s per celebrate) must land within 20% of this."
    },
    "language": { "type": "string", "const": "en" },
    "helperLanguageKeys": {
      "type": "array",
      "uniqueItems": true,
      "items": { "type": "string", "pattern": "^[a-z0-9_]+$" },
      "description": "Localization.key_for(<English line>) keys the helper-language (Thai by default) second line may translate. Each key must derive from a line that this lesson actually speaks."
    },
    "steps": {
      "type": "array",
      "minItems": 3,
      "maxItems": 24,
      "items": { "$ref": "#/definitions/step" },
      "description": "First step is a teach (the greeting), last step is the single celebrate. Every ask step is followed by a teach or another ask, never by nothing."
    },
    "completion": { "$ref": "#/definitions/completion" }
  },
  "definitions": {
    "shortLine": { "type": "string", "minLength": 1, "maxLength": 160 },
    "questionLine": { "type": "string", "minLength": 1, "maxLength": 120 },
    "step": {
      "type": "object",
      "additionalProperties": false,
      "required": ["stepId", "kind"],
      "properties": {
        "stepId": { "type": "string", "pattern": "^[a-z0-9_]+$", "description": "Unique within the lesson." },
        "kind": { "type": "string", "enum": ["teach", "ask", "celebrate"] },
        "teachText": { "$ref": "#/definitions/shortLine", "description": "What Aliz says on a teach or celebrate step." },
        "questionText": { "$ref": "#/definitions/questionLine", "description": "The question Aliz asks on an ask step." },
        "visualAssetId": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9_]*$",
          "description": "Flashcard shown for the step. Must be listed in res://content/tutor/assets_allowlist.json. Optional on teach/celebrate; every ask step in a full lesson carries one."
        },
        "expectedAnswers": {
          "type": "array",
          "minItems": 1,
          "maxItems": 12,
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1, "maxLength": 40, "pattern": "^[a-z0-9][a-z0-9 ']*$" },
          "description": "Lower-case English answers. The FIRST entry is the canonical answer Aliz teaches on the third attempt; the rest are synonyms and phrase forms (\"an apple\", \"it's an apple\"). No transliterations."
        },
        "objective": { "type": "string", "minLength": 1, "maxLength": 120, "description": "What the child practises; shown to grown-ups, never scored." },
        "hint": { "$ref": "#/definitions/shortLine", "description": "Spoken on the second unclear/incorrect attempt (give_hint)." },
        "encouragement": { "$ref": "#/definitions/shortLine", "description": "Spoken on the first unclear/incorrect attempt (retry). Kind, short, never says wrong." },
        "successLine": { "$ref": "#/definitions/shortLine", "description": "Spoken when the answer matches." },
        "answerLine": { "$ref": "#/definitions/shortLine", "description": "Optional. Spoken on the third attempt instead of the generated \"It's <answer>! Say <answer>.\"" },
        "spokenLineId": { "type": "string", "pattern": "^[a-z0-9_]+$", "description": "Optional owner voice-pack line id (for example aliz_006_good_job). When absent the synthesis provider falls back to TTS." }
      }
    },
    "completion": {
      "type": "object",
      "additionalProperties": false,
      "required": ["stars", "stickerId"],
      "properties": {
        "stars": { "type": "integer", "minimum": 1, "maximum": 5, "description": "Awarded once per lesson by LessonEngine when the celebrate step is passed. Never reduced, never withheld." },
        "stickerId": { "type": "string", "pattern": "^[a-z][A-Za-z0-9_]*$" },
        "celebrationLine": { "$ref": "#/definitions/shortLine" }
      }
    }
  },
  "x-requiredByKind": {
    "teach": ["teachText"],
    "ask": ["questionText", "expectedAnswers", "objective", "hint", "encouragement", "successLine"],
    "celebrate": ["teachText"]
  },
  "x-forbiddenByKind": {
    "teach": ["questionText", "expectedAnswers", "hint"],
    "ask": ["teachText"],
    "celebrate": ["questionText", "expectedAnswers", "hint"]
  },
  "x-durationModel": {
    "teachSeconds": 6,
    "askSeconds": 12,
    "askRetryAllowanceSeconds": 10,
    "celebrateSeconds": 10,
    "toleranceRatio": 0.2
  }
}
